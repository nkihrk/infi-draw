import { SlideBrushSizeDirective } from './slide-brush-size.directive';

describe('SlideBrushSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new SlideBrushSizeDirective();
    expect(directive).toBeTruthy();
  });
});
